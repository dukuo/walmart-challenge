import {
    Button
} from '@material-ui/core'

const SearchProduct = () => {
    return <Button
        color="primary"
        variant="contained"
        onClick={() => console.log("YAY")}>Howdy</Button>
}

export default SearchProduct